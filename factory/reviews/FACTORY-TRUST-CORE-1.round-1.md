# FACTORY-TRUST-CORE-1

Finding: FACTORY-TRUST-CORE-1
Reviewer: finding-closure-reviewer subagent
Reviewer Agent Type: finding-closure-reviewer
Reviewer Agent ID: ac5df597855073dfa
Reviewed Commit: 382f347a28b61cbcd683fb56913e6a01498aff46 (base e57f97b31cdb4be03dfa798b34066a74268dfdcb)
Reviewed Scope Hash: sha256:4a9a17a48ca0ca32de59d88e1950ca2e660329b309a0cb3177be8a2293c47043
Result: PASS
Root Cause Addressed: Yes. The documented root cause ("an assertion was checked instead of a binding at every handover") is addressed by real bindings in code, not by documentation: F-01 factory/guards/validate-finding.py:336-350 (canonical round path only), :357-376 (realpath must stay inside resolved factory/reviews/, symlink escape rejected), :379-393 (the artifact must pass validate-review.py in full, exit code inspected); F-02 validate-finding.py:396-402 plus validate-review.py:207-220 (filename, Finding: field and an existing finding file must agree); F-03 validate-finding.py:434-451 (scope hash recomputed and compared; ScopeHashError refuses closure) and .claude/hooks/subagentstop-write-review.py:281-288 (no artifact written at all when the hash cannot be computed); F-04 validate-finding.py:404-422 (highest round, rounds 1..N gapless) and :454-466 (an earlier non-PASS round with the identical scope hash blocks the later PASS), plus subagentstop-write-review.py:200-212 and :296-302 (next free round, explicit refusal to overwrite); F-05 factory/guards/validate-control-plane.py:60-93,171-190 wired into factory/guards/run-factory-checks.py:144-166,187-190 and therefore into every CI run via .github/workflows/factory-ci.yml:45-46; F-06/F-08 factory/guards/gh_evidence.py:119-165 (pessimistic aggregation over same-named runs, pagination incompleteness is api_error, neutral/skipped/stale/cancelled/no-conclusion are explicitly not a pass) and :185-208 with factory/scripts/gh-query.sh:193-219 (expected head SHA is mandatory, checked locally and passed to the merge API); F-07 factory/scripts/gh-api.sh:149-175 (HTTP status captured, exit 3/4, body still printed) and gh-query.sh:78-87 (render returns 4 when the API layer failed but the renderer did not); F-09 validate-finding.py:149-156,254-284 (Severity mandatory from ANALYZED, P0 restricted to OPEN/ANALYZED/EXPERT_REVIEW_REQUIRED, reachable through every status path because _check_severity runs before and independently of all other checks in validate_finding at :239); F-17 gh-api.sh:58-95 (normalization across all documented remote spellings plus a hard OWNER/REPO validation before any request). The scope-hash purity defect is fixed at its actual cause and not hidden: sys.dont_write_bytecode is set by the two importers before the import (validate-finding.py:90, subagentstop-write-review.py:97), which is the only place it can take effect, factory/guards/scope_hash.py:63-66 still excludes exactly factory/findings/ and factory/reviews/ and nothing else, and no path-based suppression was introduced.
Regression Evidence Checked: I cross-checked every test name in the build order's Red Regression Evidence table against the current factory/guards/test_trust_core.py: all 17 named failures exist verbatim (ReviewArtifactPathTests 5, ReviewIdentityTests 2, ScopeBindingTests 2, ReviewRoundTests 2, SeverityGateTests 5, CanonicalRunnerTests 1) and each still asserts a rejection via assertRejected (test_trust_core.py:218-226), whose message "guard accepted what it must reject" matches the quoted red output verbatim. Test counts also reconcile: 29 tests in the six original classes and 8 in ScopeHashPurityTests = 37, matching the claimed 29-then-37. The four purity tests claimed red without the fix (test_the_finding_validator_writes_no_bytecode, test_git_add_after_measuring_tracks_no_bytecode, test_scope_hash_is_unchanged_by_measuring_it, test_the_review_binding_survives_a_measurement) all exist at test_trust_core.py:564-597, and the four over-repair guards at :560-562 and :601-637 do assert what the build order says (a real code change still moves the hash; a deliberately tracked .pyc stays inside the hash; the hashed-file count equals git ls-files minus exactly the two documented prefixes). The purity fixture deliberately removes an inherited PYTHONDONTWRITEBYTECODE (:511) so the tests cannot pass vacuously. No assertion was softened to a smoke test. Deleted tests: the only removals in the diff are factory/guards/test_run_factory_checks.py's two closure tests (diff lines 3751, 3800), replaced by stricter round-based equivalents (diff 3760-3820), and factory/guards/test_validate_finding.py's three tests (diff 4674, 4702, 4721) that asserted the UNSAFE accepting behaviour (absolute foreign path with Result: PASS accepted); these are explicitly inverted into rejections (diff 4677-4700, 4740-4749) with the rationale documented in that file's module docstring. The one genuine loosening I found is minor: test_validate_finding.py's test_ready_for_closure_with_review_fail_is_rejected lost its assertIn("Result 'FAIL'") and now only asserts exit 1 (the rejection now happens earlier, on the non-canonical path); the FAIL-specific rejection is still covered by test_trust_core.py:380-383 test_latest_round_must_be_pass. I could not execute any test: read-only review, no Bash. The red-before/green-after claims themselves therefore rest on the quoted output plus the external CI runs, not on my own execution.
Guard Evidence Checked: factory/guards/validate-control-plane.py's rules (:60-73) do cover every category its docstring and .claude/rules/factory-workflow.md claim, and factory/guards/test_control_plane.py:33-43,114-124 pins one file per category plus explicit negative fixtures (findings, build orders, product code, reviews must NOT be in the manifest) and the real attack shapes (:128-183, weakened guard, changed CI workflow, changed gh-query.sh, changed hook, changed agent definition, changed CLAUDE.md, added file, removed file). The manifest at factory/control-plane.sha256 lists 30 files, matches the claimed count, and every entry is genuinely control plane; four of the six hashes quoted in the build order's live guard output match the manifest exactly (finding-closure-reviewer.md cb3872b9ce57, test_stop_validate_findings.py adc276021592, settings.json db4d52581595, SKILL.md 18b6452a37c9), and the two that differ (subagentstop-write-review.py, test_subagentstop_write_review.py) are consistent with the later scope-hash-purity commit having changed those two files again and the manifest having been re-stamped afterwards. factory/guards/test_gh_evidence.py covers every claimed GitHub failure mode without a network, including queued/in_progress/cancelled/skipped/neutral/stale/timed_out/no-conclusion, same-named runs in all combinations, incomplete pagination, six HTTP error classes, empty and non-JSON bodies, the merge precheck (stale head, already merged, closed PR, api_error) and all documented origin spellings; GhQueryWiringTests (:303-362) exercises the real gh-query.sh against a stubbed gh-api.sh and asserts the exit codes actually propagate. Two documentation-vs-rule gaps in the guard: (1) the directory rules are suffix-filtered, so e.g. factory/scripts/*.py or factory/guards/*.sh would not be manifest-covered, which the docstring does not state (low impact: such a file cannot be invoked without changing a covered file); (2) .claude/rules/factory-workflow.md and CLAUDE.md list ".claude/settings*.json" while CONTROL_PLANE_FILES only names .claude/settings.json (settings.local.json is gitignored and covered separately by the preflight, so this is cosmetic). I could not recompute any sha256 with read-only tools; the manifest's correctness for this commit rests on the CI run, not on my own computation.
Scope Checked: 33 changed files. All but two are inside the build order's Scope block. Outside it: (a) .gitignore -- not listed in Scope, but it is the defense-in-depth half of the scope-hash purity fix and is explicitly described in the build order's "Nachtrag" section (lines 173-174); its content (diff 1316-1324) is exactly __pycache__/ and *.py[cod]; (b) .claude/hooks/test_stop_validate_findings.py -- a control-plane path, not listed in Scope, changed only to make its fixture a real git repo with a stamped manifest (required because the canonical runner now runs the control-plane guard) and to ADD two tests (P0 in the normal lifecycle is blocked, control-plane drift blocks the Stop hook; diff 399-414). Both are related to this change and strengthening; neither is an unrelated control-plane edit riding along. Still, the Scope block calls itself "Erlaubt und abschliessend", so this is a real if minor scope deviation that should have been recorded in the build order. As a FACTORY_CHANGE I reviewed the entire control-plane diff: settings.json (deny + denyWrite extended, nothing removed), factory-workflow.md (P0 gate, the eight closure conditions, append-only rounds, FACTORY_CHANGE section, all matching the code), CLAUDE.md, SKILL.md (Step 4 forbids hand-authoring an artifact; Step 7 requires a fresh required-check for the NEW head SHA before merge), the reviewer agent definition (adds the "do not output the three hook-set fields" rule), factory-preflight.sh (guards/scripts write access REMOVED from the required-allow list and MOVED to too_broad, control-plane manifest check added), gh-api.sh, gh-query.sh, factory-ci.yml (three new test modules added to CI, nothing removed). factory/control-plane.sha256 was re-stamped deliberately and every one of its 30 entries corresponds to a file that was meant to exist as control plane. No claim anywhere to close F-10..F-16 or F-18..F-21; test_factory_preflight.py:58 even states explicitly that F-14 is not in this package.
Remaining Risks: (1) Local-layer gap contradicting CLAUDE.md's own text: CLAUDE.md asserts "Der Schutz ist zweischichtig" over a block that includes CLAUDE.md itself, but .claude/settings.json has no Edit(/CLAUDE.md)/Write(/CLAUDE.md) deny entry and no "./CLAUDE.md" denyWrite entry, and factory/scripts/factory-preflight.sh does not require one; factory/control-plane.sha256 is likewise locally writable. So for those two files only the server-side manifest layer exists. Consequence is detection without prevention (CI fails, SKILL.md Step 1.4 tells the agent to revert rather than re-stamp), and the combined "change a control-plane file AND re-stamp in one commit" case is the openly documented bootstrap limit -- but the documentation currently overstates the local layer. (2) .claude/hooks/test_subagentstop_write_review.py's new test_settings_json_denies_writes_to_the_whole_control_plane (diff 860-885) does not check CLAUDE.md or the manifest, so its name overstates its coverage. (3) The manifest's suffix filters leave factory/guards/*.sh and factory/scripts/*.py uncovered (see Guard Evidence). (4) The finding's CI Evidence binds to head c61a3a6, while the reviewed commit is 382f347; per SKILL.md Step 7 a fresh required-check for the final head is mandatory before merge, and I cannot verify that read-only. (5) Everything below the tool-permission layer stays outside this design (a local shell rewriting both a review round and the code can restamp), which scope_hash.py:32-39 and the reviews README state honestly. (6) I executed nothing: no test run, no hash computation, no CI query -- red/green and manifest correctness rest on the quoted evidence and the external CI runs.
Findings And Objections: Three concrete objections, none of which reopens a door this change closed. (a) The local protection layer does not match its own documentation: CLAUDE.md lists CLAUDE.md and factory/control-plane.sha256 as protected control plane and claims two-layer protection, but .claude/settings.json:3-41 contains no deny or denyWrite entry for either, and factory-preflight.sh's required lists (diff 6020-6053) do not require one. This should be closed in package 2 by adding Edit(/CLAUDE.md)/Write(/CLAUDE.md) to permissions.deny and ./CLAUDE.md to sandbox.filesystem.denyWrite, or by weakening the sentence in CLAUDE.md to say what is actually true. (b) Honesty item (b) is disclosed but understated and internally inconsistent: the build order's Bootstrap section point 3 says "Vier Dateien der Kontrollebene konnten vom implementierenden Agenten nicht geschrieben werden" and then lists five path entries covering six manifest files plus factory/reviews/README.md, while the Green Runtime Fix Evidence section on the same page correctly speaks of "die sechs geschuetzten Control-Plane-Dateien" and names all six. It also does not spell out that the later scope-hash-purity round required .claude/hooks/subagentstop-write-review.py (and its test) to be applied externally a second time -- which the manifest hashes (3cac7ddd / 8d98a894 versus the eb2faf43 / 6dfe7f86 quoted in the build order output) indicate did happen. Nothing is concealed, but the count and the timeline are wrong and should be corrected in the build order. (c) Two changed files (.gitignore, .claude/hooks/test_stop_validate_findings.py) fall outside the build order's Scope block, which declares itself conclusive; both are directly motivated by this change and strengthen rather than weaken anything, but the Scope block should have been updated. Honesty item (a), the bootstrap boundary of the manifest, I judge honest and complete: it is stated three times independently (build order Bootstrap points 1-3, validate-control-plane.py:37-45, .claude/rules/factory-workflow.md "Ehrliche Grenze"), it does not claim the manifest validated itself for this commit, and it correctly locates the manifest's value at the next finding. I found no path by which a stale, re-used or foreign PASS can close a finding (path, identity, round order, gaplessness, verdict, scope hash and same-hash-non-PASS are all enforced and all fail closed), no way for an API error, an absent check or a moved head SHA to be read as success, no reachable status path around the P0 stop, and no test or control weakened to make anything green.
