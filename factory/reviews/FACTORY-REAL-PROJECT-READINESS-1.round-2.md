# FACTORY-REAL-PROJECT-READINESS-1

Finding: FACTORY-REAL-PROJECT-READINESS-1
Reviewer: finding-closure-reviewer subagent
Reviewer Agent Type: finding-closure-reviewer
Reviewer Agent ID: a78966cd374e836f0
Reviewed Commit: 3780c360c0dc059e64ca7eadfeda11c48df8a4c5 (Branch factory-change/real-project-readiness-1, Basis 682aa95d211e1cc30cb80e182aed880adfbf0102); einzige Arbeitsbaum-Abweichung: CI-Evidence-Feld in factory/findings/FACTORY-REAL-PROJECT-READINESS-1.md auf den neuen Head-SHA aktualisiert (uncommitted, ausserhalb des Scope-Hashes)
Reviewed Scope Hash: sha256:9b25f7085f789a7c6f2eceac2f7ed68a4b6290a3135a0dbb7f753d5c35ba4fef
Result: PASS
Root Cause Addressed: Ja, an allen drei Stellen, selbst nachgeprueft. F-18: factory/guards/finding_format.py split_sections (Zeilen 143-179) schliesst den Metadatenblock beim ersten Heading und setzt metadata_closed nie zurueck; Body-Zeilen werden bei in_analysis=False verworfen, ein Status:/Severity: in ## Befund oder ## Analyse ist damit unerreichbar statt nur nachrangig. _strip_fenced_blocks (Zeilen 82-100) entfernt Fenced Code Blocks (Backtick- und Tilde-Zaeune) vor jeder Feldsammlung; _collect_fields (103-140) behandelt eingerueckte Zeilen als Fortsetzung, sodass FIELD_RE nur an Spalte 0 greift; FIELD_RE bindet non-greedy an den ERSTEN Doppelpunkt, weshalb eine Zeile 'Root Cause: ... Status: CLOSED' den Feldnamen 'Root Cause' ergibt. validate-finding.py:221-223 macht status=None zum harten Fehler statt zum Default; _check_required_fields (329-355) prueft jede Zeile des mehrzeiligen Werts und meldet die Zeilennummer. Genau ein Parser: Grep ueber das gesamte Repository findet 'def parse_finding' nur in finding_format.py:182 und Importe nur in validate-finding.py:93 und validate-build-order.py:73; kein Hook parst selbst (.claude/hooks/*.py enthalten 'Status:' ausschliesslich in Fixtures). F-21: project_tests.py enthaelt keine Datei-Heuristik (test_adapter_contains_no_python_test_discovery pinnt das ueber rglob/glob), classify (Zeilen 481-492) macht TEMPLATE_WITHOUT_PRODUCT zum einzigen Null-Test-Pass, main fuehrt jede Suite aus, sammelt alle Fehlschlaege und meldet EXIT_TESTS_FAILED, run_suite liefert bei FileNotFoundError/OSError EXIT_BLOCKED statt 0; shell=True existiert nirgends, Metazeichen werden bereits in parse_config abgelehnt. F-15: create-finding-worktree.sh fuehrt die Schreibprobe in Zeilen 174-196 aus, der ausfuehrbare 'git worktree add'-Aufruf steht erst in Zeile 205 -- die Blockade ist praeventiv, ohne Mutation.
Regression Evidence Checked: Mechanisch gegengeprueft, nicht uebernommen. F-18: das im Diff (Zeilen 3173-3209) entfernte alte parse_finding matchte STATUS_LINE_RE gegen die bereits ge-stripte Zeile, weshalb ein eingerueckter zitierter Ticketblock oberhalb der kanonischen Felder zwingend 'OPEN'/'P1' liefert (Fall 1), Feldwerte an der Zeilengrenze endeten (Fall 2) und die Platzhalterpruefung nur die erste Zeile sah (Fall 3) -- die zitierte rote Ausgabe ist damit reproduzierbar begruendet. Die aktuellen Assertions in test_finding_format.py decken genau diese drei Faelle ab und zusaetzlich die gefaehrliche Richtung (test_quoted_severity_p1_does_not_switch_off_the_p0_hard_stop ruft vf._check_severity real auf und verlangt ein nicht-leeres Fehlerergebnis); die Meldung 'in Zeile 2' des Tests entspricht woertlich der Formatierung in validate-finding.py:345-353. F-21: die zitierte rote Zeile 'Keine Tests unter ... gefunden -- nichts zu pruefen.' plus EXIT=0 steht woertlich im entfernten Code (Diff 1875-1876); der Gegentest test_non_python_product_with_a_failing_test_is_not_reported_as_green verlangt assertNotEqual(code, EXIT_OK) und den Blocker-Zustand, test_second_suite_red_makes_the_whole_run_fail belegt ueber eine Marker-Datei, dass die spaetere rote Suite wirklich lief. F-15: die zitierte SANDBOX_WORKTREE_INCOMPATIBLE-Ausgabe entspricht woertlich den echo-Zeilen 189-192 des Skripts, und test_script_blocks_before_attempting_the_worktree vergleicht die Positionen von 'worktree-sandbox-probe' (Zeile 183) und dem eingerueckten 'git worktree add --no-track' (Zeile 205). Keine Assertion ist gegenueber dem Bauauftrag abgeschwaecht; in den sieben mitgeaenderten Testdateien enthalten alle Hunks ausschliesslich Hinzufuegungen (Fixture-Modullisten, project-tests.conf in Fixtures mit Produktcode), kein Testfall wurde entfernt, keine Pruefung deaktiviert, keine Suppression eingefuehrt. Abweichung zum Record: die zitierten Laufzahlen (Ran 355 / Ran 56) stammen aus dem Stand VOR Runde 2; der gepruefte Baum enthaelt einen Test mehr (test_raw_git_worktree_is_not_a_required_routine_grant), der laut Auftraggeber lokal 356/57 ergibt -- diese Zahlen stehen nicht im Finding und nicht im Bauauftrag. Nicht mit Lesewerkzeugen verifizierbar und ausdrueckliche Grenze dieses Reviews: alle Testlaeufe, SANDBOX_VERIFICATION, die Actions-Laeufe 31898242686/31898239077 und die SHA-256-Werte des Manifests -- ich habe kein Bash und konnte weder Tests noch Git noch die GitHub-API ausfuehren.
Guard Evidence Checked: (1) validate-control-plane.py:70-80 nimmt factory/project-tests.conf in CONTROL_PLANE_FILES auf, factory/control-plane.sha256:47 stempelt sie; da der Guard auch Hinzufuegungen und Entfernungen erkennt, ist die Manifestvollstaendigkeit erzwungen. Der Manifest-Diff umfasst exakt 23 Zeilen, die den 23 geaenderten Control-Plane-Dateien entsprechen; .github/workflows/factory-ci.yml behaelt a72c1c83... und ist tatsaechlich unveraendert; keine zusaetzliche Datei wurde still mitgestempelt, keine entfernt. (2) Reihenfolge in .github/workflows/factory-ci.yml:53-57: run-factory-checks.py mit dem Control-Plane-Guard laeuft vor run-project-tests.py, eine manipulierte Konfiguration ohne neu gestempeltes Manifest scheitert also, bevor ein Kommando ausgefuehrt wird. (3) run-factory-checks.py:223-275 ruft project_tests.evaluate(REPO_ROOT) und laesst REAL_PROJECT_WITHOUT_TEST_CONFIGURATION den Gesamtlauf scheitern; dass die Suiten dort nicht ausgefuehrt werden, ist begruendet und CI-seitig abgedeckt. (4) .claude/settings.json enthaelt ausschliesslich zwei Ergaenzungen (Edit/Write auf /factory/project-tests.conf sowie ./factory/project-tests.conf in denyWrite); nichts entfernt, nichts verbreitert. (5) Der Preflight-Eingriff aus Runde 2 ist korrekt und verschaerfend: der Eintrag fuer 'git worktree' wurde nur aus 'required' entfernt (factory-preflight.sh:377-383); weil known aus required abgeleitet wird, wird eine bestehende solche Freigabe jetzt als UNKNOWN gemeldet und fuehrt zu BLOCKED (Zeilen 455-457, 470-471, 492-494). Es wurde keine Berechtigung erteilt und .claude/settings.local.json nicht angefasst (dort existiert der Eintrag ohnehin nicht). Ein legitimer Ablauf wird nicht blockiert: die Routine setzt selbst kein 'git worktree' ab, und create-finding-worktree.sh laeuft als eine allowlistete Skriptausfuehrung. (6) Rueckwaertskompatibilitaet real geprueft: alle vier Findings (EXAMPLE-FINDING, TRUST-CORE, OPS-ROBUSTNESS, REAL-PROJECT-READINESS) haben Titel, Status und Severity im kanonischen Block und Analysefelder an Spalte 0 -- der neue Parser kann sie nicht anders lesen. (7) Schwachstelle im Doku-Guard: PARALLELISM_CLAIM_RE in test_worktree_protection.py:76-80 matcht nur 'parallel sicher', 'sicher parallel' oder die Wendung mit 'unterstuetzt' ohne Umlaut. Die FRUEHERE Formulierung in CLAUDE.md ('Fuer echt gleichzeitige Arbeit ... einen eigenen Worktree pro Finding-Branch verwenden') und die im Repository durchgaengige Umlaut-Schreibweise wuerden NICHT matchen.
Scope Checked: Tatsaechlich geaendert sind 29 Dateien. Die nachgebesserte Scope-Liste des Bauauftrags (Zeilen 70-105) nennt 29 Pfade und deckt alle 28 agentenseitig geaenderten Dateien ab -- die neun in Runde 1 beanstandeten Pfade sind vollstaendig und namentlich nachgetragen. Zwei Abweichungen, beide unkritisch: .github/workflows/factory-ci.yml steht in der Liste, wurde aber nicht geaendert (Manifest-Hash unveraendert); factory/reviews/FACTORY-REAL-PROJECT-READINESS-1.round-1.md ist im Diff, steht aber nicht in der Liste -- diese Datei stammt ausschliesslich aus dem SubagentStop-Hook, liegt ausserhalb des Scope-Hashes, ist inhaltlich identisch mit dem FAIL-Artefakt in factory/reviews/ und wurde nicht nachtraeglich veraendert (append-only gewahrt). Keine geschlossenen Findings der Pakete 1 und 2, keine fremden Bauauftraege und keine fruehere Review-Runde wurden angefasst. Als FACTORY_CHANGE ist die Kontrollebene in Scope; ich habe den gesamten Control-Plane-Diff gelesen und keine unzusammenhaengende Mitnahme gefunden -- jede Aenderung ist auf F-15, F-18 oder F-21 zurueckfuehrbar.
Remaining Risks: (1) Bekannt und ausdruecklich nicht repariert: bei doppeltem Feldnamen im kanonischen Block gewinnt der letzte Treffer (_collect_fields, finding_format.py:103-140); zwei Severity-Zeilen koennten ein P0 zu P1 aufloesen. Nicht closure-blockierend, weil es keine Faehigkeit hinzufuegt (wer P1 hinschreiben will, schreibt es einmal hin), im sichtbaren kanonischen Bereich liegt und kein Fliesstext-Bypass ist -- als Punkt fuer den Re-Audit gewuerdigt. (2) tracked_product_files gibt ohne .git-Verzeichnis eine leere Liste zurueck (project_tests.py, Diff-Zeilen 1451-1452); ein Nicht-Git-Baum mit Produktcode und ohne Konfiguration waere TEMPLATE_WITHOUT_PRODUCT. Im Kommentar offengelegt und in der Factory (Preflight verlangt Git, CI checkt mit .git aus) nicht erreichbar. (3) Eine Projektstruktur, deren gesamter Produktcode unter factory/, .claude/ oder .github/ liegt, gilt bei 'mode: template' als produktfrei; die Gegenmassnahme ist die Deklaration 'mode: real-project', die dann ohne Suite hart blockiert. (4) suite.workdir wird ohne Containment-Pruefung aufgeloest (run_suite), ein Workdir mit Elternverzeichnis-Anteilen waere moeglich -- die Datei ist Control Plane, daher nur theoretisch. (5) F-15/B ist Spezifikation plus Guardrail, keine harte Erzwingung: ein manuelles 'git worktree add' ausserhalb der Projektwurzel bleibt technisch moeglich; das ist in CLAUDE.md offen benannt. Die Schreibprobe im Skript benutzt relative Pfade und greift nur, wenn das Skript aus der Repo-Wurzel gestartet wird (dokumentierte Konvention). (6) run-factory-checks.py importiert jetzt project_tests, setzt aber als einziger Einstiegspunkt kein sys.dont_write_bytecode; ein __pycache__ in factory/guards ist durch .gitignore ungetrackt und damit scope-hash-neutral, weicht aber von der sonst durchgehaltenen Konvention ab. (7) Nicht mit Lesewerkzeugen entscheidbar: Testlaeufe, Sandbox-Beobachtung, Manifest-Hashes und die externe CI.
Findings And Objections: Die drei Beanstandungen aus Runde 1 sind vollstaendig behoben: (a) die Scope-Liste nennt jetzt alle neun nachgetragenen Pfade mit Begruendung (Bauauftrag Zeilen 94-123) und deckt den Diff ab; (b) factory/README.md:81-85 nennt die Option --project-dir und die Annahme app/**/test_*.py ausdruecklich als entfallen; (c) CLAUDE.md:23-27 ersetzt die app/-Konvention und benennt die frueher dort stehende Aussage als ueberholt. Verbleibende, nicht closure-blockierende Einwaende: (1) UEBERZOGENE GUARD-BEHAUPTUNG: CLAUDE.md sagt zu test_worktree_protection.py, es schlage fehl, wenn irgendein Dokument die Parallelitaetsbehauptung wieder einfuehre, und die Klassen-Docstring sagt, keine unbewiesene Parallelitaetsbehauptung duerfe im Repository existieren. PARALLELISM_CLAIM_RE (test_worktree_protection.py:76-80) wuerde weder die FRUEHERE Formulierung dieser Behauptung noch die im Repository uebliche Umlaut-Schreibweise treffen. Das ist genau die Klasse 'Dokumentation behauptet mehr als die Pruefung leistet', die dieses Paket beseitigen soll -- ohne Sicherheitswirkung, weil die eigentliche F-15-Absicherung die Sandbox, die praeventive Skript-Blockade und die Preflight-Liste sind, aber die Formulierung in CLAUDE.md sollte auf das reduziert werden, was der Regex wirklich erkennt. (2) RECORD LAEUFT DEM DIFF NACH: die Scope-Begruendung fuer factory/scripts/factory-preflight.sh nennt nur 'neue Pflichtdateien' und verschweigt die zweite, sicherheitsrelevante Haelfte derselben Aenderung (Entfernung der Routine-Freigabe fuer 'git worktree' aus der required-Liste); weder Bauauftrag noch Finding erwaehnen sie oder den zugehoerigen neuen Test. Die Aenderung selbst ist im Code ausfuehrlich begruendet und durch einen Test gepinnt, also nicht verdeckt -- aber ein Reviewer, der nur den Bauauftrag liest, erfaehrt davon nichts. (3) EVIDENCE-ZAHLEN VERALTET: Verification Evidence des Findings (Ran 355 tests) und Green Runtime Fix Evidence des Bauauftrags (Ran 56 tests) beschreiben den Stand vor Runde 2; der gepruefte Baum enthaelt 356 bzw. 57 Tests. Substanziell traegt die Aussage (0 Fehler, 0 uebersprungen, CI gruen fuer exakt 3780c36), die Zahlen sollten vor Closure auf den geprueften Stand gehoben werden. (4) KOSMETIK: der Bauauftrag enthaelt den Absatz zum Out-of-Scope zweimal (Zeilen 125-133) -- ein Artefakt der Nachbesserung. Kein Einwand gegen die inhaltliche Reparatur von F-15, F-18 und F-21, gegen die Rueckwaertskompatibilitaet, gegen die Manifest-Buchhaltung oder gegen die Preflight-Aenderung aus Runde 2; alles davon habe ich gegen den Code gehalten und konsistent gefunden.
